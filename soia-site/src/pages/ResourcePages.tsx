import { Link, useParams } from 'react-router-dom';
import { ArrowLink, useLocale } from '../components/SiteChrome';
import { CodeBlock, NumberedList, PageHero, Status } from '../components/PageBits';
import { catalog, localizedPath, posts, productShapes, t } from '../data/site';

export function DocsPage() {
  const locale = useLocale(); const zh = locale === 'zh'; const to = (path: string) => localizedPath(path, locale);
  return <><PageHero eyebrow="SOIA / DOCUMENTATION" title={zh ? <>从安装一个 Skill，<br /><em>到维护一条工作流。</em></> : <>From one Skill install<br /><em>to a maintained workflow.</em></>} lead={<p>{zh ? '文档按“理解产品 → 安装宿主 → 选择能力 → 运行验证 → 维护更新”组织。先选择你真正使用的宿主，不要求三个环境同时安装。' : 'Documentation follows: understand the product, set up a host, select capability, validate a run, then maintain it. One primary host is enough.'}</p>} aside={<div className="docs-quick"><small>QUICK START</small><CodeBlock>npx skills add soia-team/soia-open-skills -l --full-depth</CodeBlock><span>{zh ? '列出可安装能力' : 'List installable capabilities'}</span></div>} /><section className="docs-layout section-pad"><aside className="docs-nav"><p>{zh ? '在本页' : 'ON THIS PAGE'}</p>{['产品关系', '选择宿主', '安装能力', '运行验证', '边界与安全', '维护与更新'].map((item, index) => <a key={item} href={`#doc-${index + 1}`}>{String(index + 1).padStart(2, '0')} {zh ? item : ['Product relationships', 'Choose a host', 'Install capability', 'Validate a run', 'Boundaries & safety', 'Maintain & update'][index]}</a>)}</aside><div className="docs-content"><DocSection id="doc-1" label="01 / MODEL" title={zh ? 'Skill、Workflow、Plugin 与 Expert 的关系' : 'How Skill, Workflow, Plugin, and Expert relate'}><div className="doc-product-map">{productShapes.map((shape) => <div key={shape.id}><span>{shape.id}</span><b>{shape.name}</b><p>{t(shape.body, locale)}</p></div>)}</div></DocSection><DocSection id="doc-2" label="02 / HOST" title={zh ? '选择一个主宿主完成首次验收' : 'Choose one primary host for first acceptance'}><p>{zh ? 'Codex、Claude Code 和 WorkBuddy 是宿主环境，不是模型名称。首个任务只需选一个最贴近当前工作的入口；跨宿主分发需要分别验证安装、调用与更新。' : 'Codex, Claude Code, and WorkBuddy are host environments, not model names. Pick one for the first task; validate installation and updates separately across hosts.'}</p><div className="host-row"><span>Claude Code</span><span>Codex</span><span>WorkBuddy</span></div></DocSection><DocSection id="doc-3" label="03 / INSTALL" title={zh ? '先浏览，再安装领域或单个 Skill' : 'Browse first, then install a domain or one Skill'}><CodeBlock>npx skills add soia-team/soia-open-skills -l --full-depth</CodeBlock><CodeBlock>claude plugin marketplace add soia-team/soia-open-skills</CodeBlock><CodeBlock>codex plugin marketplace add soia-team/soia-open-skills</CodeBlock><p>{zh ? '每个技能详情页都有领域安装与单 Skill 安装命令，以该页公开源码为事实来源。' : 'Every Skill detail page provides domain and individual installation commands backed by public source.'}</p></DocSection><DocSection id="doc-4" label="04 / VERIFY" title={zh ? '用一个代表性输入完成首次运行' : 'Complete a first run with representative input'}><NumberedList items={(zh ? [['说明目标', '提供必要输入和合格结果，不把工具名当需求。'], ['预览计划', '可 dry-run 的动作先看预览和影响范围。'], ['运行能力', '保留人工审批点，尤其是写入、发布和删除。'], ['检查回执', '核对文件变化、产物、测试、限制与下一步。']] : [['State the outcome', 'Provide needed inputs and acceptable result, not just a tool name.'], ['Preview the plan', 'Use dry runs where possible and review impact.'], ['Run capability', 'Keep human approval for writes, publishing, and deletion.'], ['Inspect the receipt', 'Review changes, artifacts, tests, limits, and next steps.']]).map(([title, body]) => ({ title, body }))} /></DocSection><DocSection id="doc-5" label="05 / BOUNDARY" title={zh ? '能力公开，用户上下文仍归用户控制' : 'Capability is open; user context stays user-controlled'}><div className="boundary-table"><div><b>{zh ? '可以公开' : 'Safe to publish'}</b><p>{zh ? '通用方法、安装说明、测试与版本证据。' : 'Reusable method, install docs, tests, and version evidence.'}</p></div><div><b>{zh ? '保持私有' : 'Keep private'}</b><p>{zh ? '客户文件、账号凭据、内部 SOP、未公开代码与业务数据。' : 'Customer files, credentials, internal SOPs, private code, and business data.'}</p></div></div></DocSection><DocSection id="doc-6" label="06 / MAINTAIN" title={zh ? '版本、更新和失败都要留下证据' : 'Versions, updates, and failures need evidence'}><p>{zh ? '更新前确认来源与版本；更新后复跑代表性任务；宿主变更与 Skill 回归分开判断。失败时报告缺少的权限、依赖或配置，不用假结果掩盖。' : 'Confirm source and version before updating, rerun a representative task afterward, and separate host changes from Skill regressions. Report missing permissions or dependencies honestly.'}</p><ArrowLink to={to('/open/')}>{zh ? '进入能力目录' : 'Open the capability catalog'}</ArrowLink></DocSection></div></section></>;
}

function DocSection({ id, label, title, children }: { id: string; label: string; title: string; children: React.ReactNode }) { return <section className="doc-section" id={id}><p className="section-label">{label}</p><h2>{title}</h2>{children}</section>; }

export function SpecPage() {
  const locale = useLocale(); const zh = locale === 'zh';
  return <><PageHero eyebrow="SOIA / PRODUCT SPECIFICATION" title={zh ? <>能力产品体系<br /><em>公开规格 v0.1</em></> : <>Capability product system<br /><em>Public specification v0.1</em></>} lead={<p>{zh ? '这份规格定义 SOIA 公共网站上的产品语言、状态、交付层级和边界。它不是客户合同，也不包含内部报价与路线板。' : 'This specification defines public product language, status, delivery depth, and boundaries. It is not a customer contract or internal roadmap.'}</p>} aside={<div className="spec-meta"><div><small>STATUS</small><Status>Draft</Status></div><div><small>UPDATED</small><b>2026-08-03</b></div><div><small>SCOPE</small><b>Public product surface</b></div></div>} /><section className="spec-layout section-pad"><aside><p>CONTENTS</p>{['目标', '术语', '产品层级', '状态模型', '公开与私有', '验收门'].map((item, index) => <a key={item} href={`#spec-${index + 1}`}>{String(index + 1).padStart(2, '0')} {item}</a>)}</aside><div><SpecSection id="spec-1" no="01" title={zh ? '目标' : 'Goal'}><p>{zh ? '让客户在进入任何购买或合作前，明确知道拿到什么、当前状态、证据在哪里，以及什么不在范围内。' : 'Before any purchase or engagement, a user should know what they receive, current status, evidence, and exclusions.'}</p></SpecSection><SpecSection id="spec-2" no="02" title={zh ? '术语' : 'Terms'}><dl className="spec-terms">{productShapes.map((shape) => <div key={shape.id}><dt>{shape.name}</dt><dd>{t(shape.body, locale)}</dd></div>)}</dl></SpecSection><SpecSection id="spec-3" no="03" title={zh ? '产品层级' : 'Product layers'}><NumberedList items={(zh ? [['开放能力', '单个 Skill 与领域 Plugin；公共源码、文档和安装路径。'], ['组合能力', '多个 Skills 的 Workflow；写清交接、审批与失败处理。'], ['角色入口', 'Expert；角色、Skills、资料边界和推荐任务。'], ['私有交付', '在授权范围内接入 SOP、模板、代码和业务资料。']] : [['Open capability', 'Individual Skills and domain Plugins with public source and install docs.'], ['Composition', 'Workflows with handoffs, approvals, and failure handling.'], ['Role entry point', 'Experts with roles, Skills, knowledge boundaries, and suggested tasks.'], ['Private delivery', 'Authorized SOPs, templates, code, and business material.']]).map(([title, body]) => ({ title, body }))} /></SpecSection><SpecSection id="spec-4" no="04" title={zh ? '状态模型' : 'Status model'}><div className="status-model">{[['Available', zh ? '公开证据支持当前可使用。' : 'Public evidence supports current use.'], ['Preview', zh ? '形态存在，但逐宿主或长期维护仍在验证。' : 'The shape exists, but host or maintenance validation continues.'], ['Service', zh ? '按申请和确认范围交付，不公开下载。' : 'Delivered after application and scope confirmation.'], ['Roadmap', zh ? '计划方向，不作为已交付内容出售。' : 'Planned direction, not sold as delivered.']].map(([status, body]) => <div key={status}><Status tone={status === 'Available' ? 'green' : status === 'Preview' ? 'dark' : 'plain'}>{status}</Status><p>{body}</p></div>)}</div></SpecSection><SpecSection id="spec-5" no="05" title={zh ? '公开与私有边界' : 'Open / private boundary'}><div className="boundary-table"><div><b>OPEN</b><p>{zh ? '通用方法、公开 Skill、manifest、文档、测试与不含客户事实的模板。' : 'Reusable method, public Skills, manifests, docs, tests, and templates without customer facts.'}</p></div><div><b>PRIVATE</b><p>{zh ? '客户文件、内部 SOP、凭据、个人信息、未公开代码和运营决策。' : 'Customer files, internal SOPs, credentials, personal data, private code, and operating decisions.'}</p></div></div></SpecSection><SpecSection id="spec-6" no="06" title={zh ? '验收门' : 'Acceptance gates'}><ul className="check-list">{(zh ? ['输入、输出和合格条件已写清', '风险动作保留人工批准', '代表性样例完成运行', '版本、产物和限制留下回执', '私有资料未进入公共仓库'] : ['Inputs, outputs, and acceptance are explicit', 'Risky actions retain human approval', 'Representative examples complete a run', 'Versions, artifacts, and limits leave a receipt', 'Private material never enters public repositories']).map((item) => <li key={item}><span>✓</span>{item}</li>)}</ul></SpecSection></div></section></>;
}

function SpecSection({ id, no, title, children }: { id: string; no: string; title: string; children: React.ReactNode }) { return <section className="spec-section" id={id}><span>{no}</span><div><h2>{title}</h2>{children}</div></section>; }

export function BlogPage() {
  const locale = useLocale(); const zh = locale === 'zh'; const to = (path: string) => localizedPath(path, locale);
  return <><PageHero eyebrow="SOIA / FIELD NOTES" title={zh ? <>把方法写清楚，<br /><em>也把边界写清楚。</em></> : <>Write the method.<br /><em>Write the boundary too.</em></>} lead={<p>{zh ? '关于开放能力、内容系统、Agent 工作流与私有交付的产品文章。只发布已经形成证据的判断，不把路线图写成新闻。' : 'Product notes on open capability, content systems, agent workflows, and private delivery. Evidence before announcements.'}</p>} /><section className="blog-grid section-pad">{posts.map((post, index) => <Link className={index === 0 ? 'post-card is-featured' : 'post-card'} key={post.slug} to={to(`/blog/${post.slug}/`)}><div className="post-art"><span>{post.tag}</span><b>SOIA<br />FIELD<br />NOTE</b><i>0{index + 1}</i></div><div><small>{post.date} · {post.tag}</small><h2>{t(post.title, locale)}</h2><p>{t(post.summary, locale)}</p><span>{zh ? '继续阅读' : 'Read article'} ↗</span></div></Link>)}</section></>;
}

export function BlogPostPage() {
  const { post: postSlug } = useParams(); const locale = useLocale(); const zh = locale === 'zh'; const to = (path: string) => localizedPath(path, locale); const post = posts.find((item) => item.slug === postSlug) ?? posts[0];
  const sections = post.slug === 'skill-workflow-plugin-expert' ? (zh ? [['Skill 回答能力问题', '它是最小能力单元：什么情况下触发、需要什么输入、按什么步骤执行、输出什么、边界在哪里、如何验收。'], ['Workflow 回答组合问题', '它连接多个 Skills，负责先后顺序、状态交接、人工批准、失败处理与最终回执。'], ['Plugin 回答入口问题', '它让一组能力能在 Codex 或 Claude Code 中按版本安装、启停和更新。'], ['Expert 回答角色问题', '它把角色、Skills、资料范围和推荐任务组织成 WorkBuddy 等宿主里的使用入口。']] : [['Skill answers capability', 'The smallest unit: trigger, input, steps, output, boundary, and acceptance.'], ['Workflow answers composition', 'It connects Skills with state, approvals, failure handling, and receipts.'], ['Plugin answers entry point', 'It packages capabilities for installation and versioning in Codex or Claude Code.'], ['Expert answers role', 'It organizes role, Skills, knowledge scope, and suggested tasks in a host such as WorkBuddy.']]) : (zh ? [['先区分方法与上下文', '通用方法可以开源；客户文件、内部 SOP、凭据和未公开代码必须保持私有。'], ['再区分宿主与能力', 'Codex、Claude Code 与 WorkBuddy 是运行入口；Skill 和 Workflow 才是被运行的方法。'], ['最后用验收而不是口号', '真实输入、产物、测试、人工决策点和已知限制，共同构成交付证据。']] : [['Separate method and context', 'Reusable method can be open; customer files, SOPs, credentials, and private code stay private.'], ['Separate host and capability', 'Codex, Claude Code, and WorkBuddy are entry points; Skills and Workflows are the methods they run.'], ['Use acceptance, not slogans', 'Real inputs, artifacts, tests, human decisions, and known limits create delivery evidence.']]);
  return <article className="blog-article"><PageHero compact eyebrow={`${post.date} / ${post.tag}`} title={<>{t(post.title, locale)}</>} lead={<p>{t(post.summary, locale)}</p>} /><section className="article-layout section-pad"><div className="article-main"><p className="article-deck">{zh ? '真实工作不是一次聊天。它包含上下文、工具、权限、人工判断、失败处理和验收。产品语言必须反映这些差异。' : 'Real work is not one chat. It includes context, tools, permissions, human judgment, failure handling, and acceptance. Product language must reflect those differences.'}</p>{sections.map(([title, body], index) => <section key={title}><p className="section-label">0{index + 1} / {index === 0 ? 'DEFINE' : index === 1 ? 'CONNECT' : 'VERIFY'}</p><h2>{title}</h2><p>{body}</p></section>)}<div className="article-callout"><b>{zh ? '一句话总结' : 'In one line'}</b><p>{zh ? '开放能力解决“方法能不能复用”，私有交付解决“方法如何在你的上下文里安全运行”。' : 'Open capability asks whether a method is reusable; private delivery asks how it runs safely in your context.'}</p></div></div><aside className="article-aside"><div className="sticky-card"><small>NEXT</small><Link to={to('/products/')}>{zh ? '查看产品体系' : 'View product system'} →</Link><Link to={to('/open/')}>{zh ? '浏览开放生态' : 'Browse open ecosystem'} →</Link><Link to={to('/services/')}>{zh ? '了解私有交付' : 'Explore private delivery'} →</Link></div></aside></section></article>;
}

export function ShowcasePage() {
  const locale = useLocale(); const zh = locale === 'zh'; const to = (path: string) => localizedPath(path, locale);
  const items = [
    ['01', 'OPEN SKILL', 'soia-pkm-clip-web', zh ? '网页 → 可检索知识记录' : 'Web page → searchable knowledge record', '/open/pkm-vault/soia-pkm-clip-web/'],
    ['02', 'WORKFLOW', zh ? '内容生产线' : 'Content production line', zh ? '来源 → 观点 → 成文 → 视觉 → 多平台草稿' : 'Source → opinion → article → visual → channel drafts', '/solutions/content/'],
    ['03', 'PLUGIN', 'soia-pkm-vault', zh ? '一个领域插件，31 个知识能力' : 'One domain plugin, 31 knowledge capabilities', '/open/pkm-vault/'],
    ['04', 'EXPERT', zh ? '个人知识与内容资产专家' : 'Knowledge & Content Asset Expert', zh ? '角色、Skills、资料边界与推荐任务' : 'Role, Skills, knowledge boundaries, suggested tasks', '/open/experts/pkm-vault/'],
  ];
  return <><PageHero eyebrow="SOIA / SHOWCASE" title={zh ? <>不是效果图。<br /><em>是可检查的成品结构。</em></> : <>Not mockups.<br /><em>Inspectable delivery structures.</em></>} lead={<p>{zh ? '展示当前可以公开验证的 Skill、Workflow、Plugin 与 Expert 形态。客户案例只在获得授权后出现。' : 'Current public delivery shapes across Skills, Workflows, Plugins, and Experts. Customer cases appear only with authorization.'}</p>} /><section className="showcase-grid section-pad">{items.map(([id, type, title, body, href], index) => <Link className={`showcase-card art-${index + 1}`} key={id} to={to(href)}><div><span>{id}</span><small>{type}</small></div><h2>{title}</h2><p>{body}</p><i>↗</i></Link>)}</section></>;
}

export function AboutPage() {
  const locale = useLocale(); const zh = locale === 'zh'; const to = (path: string) => localizedPath(path, locale);
  return <><PageHero eyebrow="ABOUT SOIA" title={zh ? <>AI 的价值，不是多说一句答案。<br /><em>而是可靠完成一件工作。</em></> : <>AI value is not one more answer.<br /><em>It is reliable work.</em></>} lead={<p>{zh ? 'SOIA 把任务规则、领域经验、工具调用和验收条件组织成可以复用、安装、运行与审查的工作系统。' : 'SOIA organizes task rules, domain experience, tool calls, and acceptance conditions into reusable, installable, inspectable work systems.'}</p>} aside={<div className="principle-pills"><span>Local-first</span><span>Provider-neutral</span><span>Human-controlled</span><span>Evidence-backed</span></div>} /><section className="name-section section-pad"><p className="section-label">01 / THE NAME</p><div className="letter-grid">{[['S', 'System', zh ? '用系统承载事实、规则和状态。' : 'Carry facts, rules, and state in a system.'], ['O', 'Orchestration', zh ? '让角色、Skills 与工具协同。' : 'Coordinate roles, Skills, and tools.'], ['I', 'Intelligence', zh ? '让 AI 参与理解、生成与判断辅助。' : 'Use AI for understanding, generation, and decision support.'], ['A', 'Assurance', zh ? '用边界、批准和证据保障结果。' : 'Protect outcomes with boundaries, approvals, and evidence.']].map(([letter, title, body]) => <article key={letter}><strong>{letter}</strong><h2>{title}</h2><p>{body}</p></article>)}</div></section><section className="about-principles section-pad"><div><p className="section-label">02 / OUR PRINCIPLES</p><h2>{zh ? '四条原则决定怎样构建。' : 'Four principles shape how we build.'}</h2></div><NumberedList items={(zh ? [['数据优先留在原处', '文件、账号与客户资料不因为使用 Agent 就必须上传到新中心。'], ['能力不绑死单一模型', '核心规则与平台适配分开，保留切换宿主和 Provider 的空间。'], ['关键决定仍由人承担', '删除、付款、发布和授权必须有明确的人类批准点。'], ['状态由证据支持', '源码、测试、产物、运行回执和版本记录替代口号。']] : [['Data stays where it belongs', 'Using an agent does not require moving files and customer data into a new center.'], ['Capability is not model-locked', 'Separate core method and platform adapters to preserve host choice.'], ['Humans own critical decisions', 'Deletion, payment, publishing, and permission require explicit approval.'], ['Evidence supports status', 'Source, tests, artifacts, receipts, and versions replace slogans.']]).map(([title, body]) => ({ title, body }))} /></section><section className="open-private section-pad"><div><p className="section-label">03 / OPEN / PRIVATE</p><h2>{zh ? '通用能力开放，专有流程保持私有。' : 'Open reusable capability. Keep proprietary process private.'}</h2></div><div className="boundary-table"><div><b>OPEN BY DEFAULT</b><p>{zh ? '通用 Skill、安装说明、公共 manifest、不含客户事实的模板和测试方法。' : 'Reusable Skills, install docs, manifests, templates without customer facts, and test methods.'}</p></div><div><b>PRIVATE BY DESIGN</b><p>{zh ? '客户文件、内部 SOP、凭据、未公开代码、业务数据和运营决策。' : 'Customer files, internal SOPs, credentials, private code, business data, and operating decisions.'}</p></div></div></section><section className="public-proof section-pad"><div><p className="section-label">04 / WHAT EXISTS TODAY</p><h2>{zh ? '今天能验证的，和仍在建设的。' : 'What is verifiable today, and what is still being built.'}</h2></div><div className="proof-grid">{[['OPEN', 'Available', `${catalog.total} Skills · 8 Plugins`], ['EXPERT', 'Preview', '8 WorkBuddy Experts'], ['PRIVATE', 'Service', zh ? '诊断、Pilot、定制与维护' : 'Assess, pilot, customize, maintain'], ['REGISTRY', 'Roadmap', zh ? '私有插件仓库' : 'Private plugin registry']].map(([name, status, body]) => <article key={name}><small>{name}</small><Status tone={status === 'Available' ? 'green' : 'plain'}>{status}</Status><p>{body}</p></article>)}</div><div className="hero-actions"><ArrowLink className="pill-button is-dark" to={to('/open/')}>{zh ? '从开放生态开始' : 'Start with open ecosystem'}</ArrowLink><a className="pill-button" href="https://github.com/soia-team" target="_blank" rel="noreferrer">GitHub ↗</a></div></section></>;
}
